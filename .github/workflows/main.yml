name: Solar System Workflow

on: 
  workflow_dispatch:
  push:

jobs:
    unit-testing:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome Message
          run: echo "My first workflow"
        - name: List Files
          run: ls -la
        - name: Read file
          run: cat README.md        
  
